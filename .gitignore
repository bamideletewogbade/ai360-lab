/node_modules
pilot-users*.csv
/.next
/dist
/.wrangler
/.vinext
/out
/build
/tmp
.env
.env*.local
ai360-production.env

.deployment-id
# Anything derived from an env file still holds the same secrets.
.env.local.*
*.env.backup*
# Populated Postman environments and reports may contain payment credentials.
postman/*.local.postman_environment.json
postman/results/
# The nested worker installs its own dependencies; they are not source.
node_modules/
*.tsbuildinfo
next-env.d.ts
.DS_Store
.vercel

# Sentry Config File
.env.sentry-build-plugin

# Per-machine assistant permissions and command history can contain local paths
# or credential-shaped values and are never part of the product release.
.claude/settings.local.json
