/node_modules
/.next
/dist
/.wrangler
/.vinext
/out
/build
/tmp
.env
.env*.local
ai360-production.env

.deployment-id
# Anything derived from an env file still holds the same secrets.
.env.local.*
*.env.backup*
# Populated Postman environments and reports may contain payment credentials.
postman/*.local.postman_environment.json
postman/results/
# The nested worker installs its own dependencies; they are not source.
node_modules/
*.tsbuildinfo
next-env.d.ts
.DS_Store
.vercel
